# Core
fastapi>=0.110
uvicorn>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pandas>=2.0
scikit-learn>=1.3        # TF-IDF fallback + HDBSCAN clustering
python-dotenv>=1.0

# Dashboard
streamlit>=1.32
plotly>=5.20

# LLM (optional — pipeline falls back to template brief without an API key)
anthropic>=0.25

# Better embeddings (optional — pipeline falls back to TF-IDF without this)
# sentence-transformers>=2.6

# Dev
pytest>=8.0
httpx>=0.27
